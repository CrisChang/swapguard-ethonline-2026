# SwapGuard 发布交接

## 最新：回执核验与参考 Agent 对照已发布 — 2026-09-10

- [查看原网站的新对照报告](https://swapguard-ethonline-2026.swapguard.workers.dev/#evidence)。点击示例的结果现在明确区分“检查完成”“建议等待／拒绝重试”“尚未成交”，不会把预期策略结果写成系统错误。人工分析、旧回放和历史证据都保留。
- 原公开仓库功能提交 `3ed7b5bd005fb305bed3ae14ea14e2235fefd148` 已同步；该提交的 [GitHub 自动检查](https://github.com/CrisChang/swapguard-ethonline-2026/actions/runs/34449668444) 已成功。本地 306 项测试、原有 25 项参数检查、4 工具兼容测试和 5 工具错误路径检查均通过。没有新增本地浏览器视觉检查。
- 现有 Cloudflare Worker 版本 `62e6f5fc-4529-40f5-bbfa-6328c5ef4da4`，使用 `--keep-vars` 保留原配置。线上安全头、健康接口、样例／实时报价、无交易接口检查通过；新旧报告文件及本次 JS/CSS 哈希均与本地一致。直连检测曾超时，之后使用已有代理、正常 TLS 核验通过。
- 新增可选 `swapguard_verify_receipt`：从配置的 RPC 读取实际状态、Gas、到账日志及区块价格，核对指定钱包和支持的 Uniswap 参数；不是只相信调用者上报数字。它是窄范围的发送后核验，不负责签名、不是链上预算强制机制，也不保证未来最终性。
- 自建确定性参考 Agent 在本地以太坊主网分叉中实际调用 Uniswap，**4 场景 × 3 策略**，每次恢复同一快照。简单逐笔预算组完成 4/4、超预算 2/4、总 Gas 2.219848 USDC 等值；累计预算基线与 MCP 组均完成 3/4、超预算 1/4、总 Gas 1.880340。三组均只有 2/4 在原始最低到账与预算内完成。少一次成交换来少一次超预算；Gas 低估反例仍保留，不能宣传为更优路由或普遍省费。
- [逐场景报告](../public/evidence/rpc-agent-2026-09-10.md)、[完整调用／回执／账本](../public/evidence/rpc-agent-2026-09-10.json)、[接入配置及边界](AGENT_INTEGRATION.md)、[参赛文案修订](SUBMISSION_DRAFT.md) 已同步。参考 Agent 不是第三方或官方 Uniswap Agent，也不是 LLM 实盘评测；使用开发假资产，未发送主网／公共测试网交易。本轮临时 Anvil 和开发服务已停止。
- **生态：Uniswap 主攻，The Graph 仅为候选，不强凑三个。** 后续价值验证是第三方／LLM Agent 接入、预发送模拟、独立签名策略及未参与调参的市场场景。ETHGlobal 表单、奖项、已上传视频和外部反馈仍未代改。

以下是历史记录；其中“缺少回执核验”“待发布”等描述仅对应当时版本。

## Agent 与测试报告已发布 — 2026-09-10

- [原网站 · Agent 场景](https://swapguard-ethonline-2026.swapguard.workers.dev/#agents) 与 [固定测试记录](https://swapguard-ethonline-2026.swapguard.workers.dev/#evidence) 已上线，人工 Swap 分析、参数检查和原任务回放保留。
- 功能及测试数据源码：`edc663cdcbe57fc67a176a617b1117e79295e410`，已推送原公开仓库。对应 [GitHub 自动检查](https://github.com/CrisChang/swapguard-ethonline-2026/actions/runs/34444817088) 已成功。
- 新 Worker 版本：`c5461a7b-abcd-4881-90bc-7cf0c398d2cf`。发布保留了控制台现有 RPC 变量；线上样例、只读实时报价、健康检查和无交易发送接口检查均通过。
- 固定批次包含 **8 个构造场景、46 次真实本地 MCP 调用、2 次预期错误、2 次进程重启**；完整输入/返回值/合成账本、Markdown 报告和来源指纹可下载。线上文件 SHA-256 与本地一致；首页 JS/CSS 也与验证过的构建完全一致。
- 测试匹配预期不等于成功成交或真实省费。未成交、费用超预算、到账低于要求均保留，不上传访客或个人的私有账本。下一项验证重点仍是独立核验链上回执与精确交易参数绑定。
- **参赛表单、已上传视频和外部反馈未代改；没有真实资金交易。** 下方“待发布”等内容为此前本地阶段的历史记录。

## Agent 方向首次本地更新 — 2026-09-10（历史：当时待发布确认）

本次修改保留手动只读 Swap 分析、参数检查和旧任务回放；新增 Agent 接入页面及五个可运行的说明场景。定位明确为“面向已有 Swap Agent 的任务成本账本和原始约束检查”，不再使用未经证明的“最小亏损”主张。服务对象、痛点假设、输出口径和不支持的能力已写入首页、README、[接入说明](AGENT_INTEGRATION.md)和[参赛文案修订](SUBMISSION_DRAFT.md)。

新增真实本地 stdio MCP：四个工具、不可变的同任务条件、待确认状态、回执去重、失败成本保留和重启后账本恢复。MCP 输入/回执/USDC 等值费用均由调用者提供，尚未独立链上验证；没有远程 MCP 服务、钱包操作或链上强制预算。浏览器示例只是调用同一记账核心，不会连接其他 Agent。

本轮本地验证：267 项测试、25 个原有参数检查案例、真实 MCP 客户端的 14 次调用及重启恢复均通过；TypeScript/Vite 构建和现有 Cloudflare Worker 的 dry-run 通过。没有新增浏览器交互/视觉测试或真实交易。本地预览：`http://127.0.0.1:5177/#agents`。详情见 [验证记录](VALIDATION.md)。未推送仓库、未覆盖线上版本。

已有项目提交、视频上传和 Check-in 2 已由用户截图确认；公开展示页可访问。以下 2026-09-09 记录中的未完成事项是历史状态。独立 RPC 配置后，2026-09-10 审查中两种方向实时报价均通过（区块 25944202）；这不是持续可用性保证。

本次尚未修改 ETHGlobal 表单、已上传视频，也没有提交 Uniswap 外部反馈。新方向进入参赛材料前，应先发布并确认网站/公开源码版本，再更新介绍、补录实际 MCP 调用过程并重新 Submit。外部反馈完成情况与人工代码审阅仍需核实。

## 历史发布 — 2026-09-09

新版已发布到原有 SwapGuard 网站。定位改为：**自动 Swap 的任务级成本与原始约束检查**，而非新增滑点功能或承诺更省钱。这里只记录网站、代码和文案；ETHGlobal 最终提交未代办。

## 直接使用

- [公网 Demo](https://swapguard-ethonline-2026.swapguard.workers.dev/#tasks)
- [公开仓库](https://github.com/CrisChang/swapguard-ethonline-2026)
- [英文参赛文案](SUBMISSION_DRAFT.md)
- [约 3 分钟英文讲解脚本](DEMO_SCRIPT.md)
- [实现流程与证据边界](TASK_WORKBENCH.md)

进入 Task workbench，保留默认设置，确认条件后点击 Create replay task，再点击 Run remaining replay，即可看到“授权 → 首笔回滚 → 重试成功 → 最终报告”。也可以逐步操作并导出 JSON。刷新浏览器能恢复已记账费用和待处理回执，不会后台交易。

## 本次完成

- 原始最低到账量、总 Gas 预算、截止时间、最多尝试次数与执行偏好一次确认，重试时不清零。
- 授权成功后再次检查；失败 Gas 继续扣减；同一回执不重复记账，冲突回执拒绝；预算不足、超过次数或超时终止。
- 十个明确标注的自建场景，包括等待后错过价格、Gas 上涨、反复失败和报价不可用。未成交不会包装成节约。
- 首页、README、项目简介、实现说明、演示脚本、反馈文档和 AI 使用披露同步改写。
- 218 项单元/API/证据测试、25 个已有参数检查案例通过。本地 19 项浏览器测试通过，两项实时测试默认跳过。公网 19 项确定性浏览器测试通过；两项实时请求曾返回 502，详见验证记录。
- 实际本地分叉合约完成一次授权、一次人为诱发回滚和一次成功重试，三笔实际回执接入同一账本。仅使用 Anvil 假资金，节点已停止。

当前 Worker 版本：e67f7895-ec8d-4feb-b986-b3660d85eb3c（包含后续 RPC 错误处理修复）。未修改其他项目。

## 必须保留的区别

**网站闭环是模拟回放，不是自动实盘。** 真实主网只读报价仍是独立面板；没有连接你的钱包或发送主网交易。固定预算是发出前的估算条件，不是链上强制总支出上限。

默认网站回放：0.04 WETH、最低 100.195216 USDC、Gas 预算 3 USDC 等值；授权一次、Swap 尝试两次，构造路径的总 Gas 为 1.743131 USDC 等值。独立本地分叉证明固定 1 gwei，费用为 0.807383 USDC 等值；二者 Gas 路径不同，不能拿来当省费比较。池手续费已经体现在报价中，不能再重复扣。

首轮冻结试验依然保留费用优先方案 28/48 成交、立即执行 34/48 的负面结果。不保证更高成交率，也未证明优于 Uniswap 官方智能路由。

**实时报价仍需独立 RPC：** 后续排查修正了备用节点批量请求超过免费限制的问题，并改为独立单条请求和脱敏日志。云端进一步确认收到上游 429 限流，现在明确返回 503 和重试提示，不拿样例价格代替。232 项单元/API/证据测试通过，但这不等于线上行情已经恢复；需要配置 `ETHEREUM_RPC_URL` 后重新验证。回放不依赖 RPC，其他项目未改。完整历史见 [VALIDATION.md](VALIDATION.md)。

本届 Uniswap 奖项公开规则未要求主网真金白银交易；当前是主网只读行情、模拟网页回放和本地分叉假资金执行，不是公共测试网交易。详见 [网络与参赛边界](NETWORK_REQUIREMENTS.md)。

## 接下来参赛侧仍需做

1. 本人审阅代码和结果，能解释实现，保留 AI 使用说明。
2. 录制并上传符合本届规则的视频，参考新版脚本。
3. 提交 Uniswap 外部反馈表；FEEDBACK.md 文件不等于已提交。
4. 把新文案、截图、视频和已验证链接更新到 ETHGlobal，选择实际符合的奖项，最终 Submit 并核验确认。
5. 核实个人 Check-in；报名和押金确认不等于完成 Check-in。

没有主网交易、审计、真实客户或比赛获奖的新增主张。
